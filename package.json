{
  "name": "fractal-subgraph",
  "license": "UNLICENSED",
  "version": "0.0.4",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ fractal",
    "create-local": "graph create --node http://localhost:8020/ fractal",
    "remove-local": "graph remove --node http://localhost:8020/ fractal",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 fractal",
    "deploy:test": "graph deploy --node https://api.studio.thegraph.com/deploy/ fractal-test",
    "create-local:test": "graph create --node http://localhost:8020/ fractal-test",
    "remove-local:test": "graph remove --node http://localhost:8020/ fractal-test",
    "deploy-local:test": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 fractal-test",
    "test": "graph test",
    "lint": "eslint src/",
    "lint:fix": "eslit src/ --fix"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.36.1",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.47.0",
    "@typescript-eslint/parser": "^5.47.0",
    "eslint": "^8.22.0",
    "eslint-config-airbnb-typescript": "^17.0.0",
    "eslint-config-next": "^13.2.4",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-prettier": "^4.2.1",
    "matchstick-as": "0.5.0",
    "prettier": "^2.7.1"
  }
}
