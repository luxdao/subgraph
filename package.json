{
  "name": "fractal-subgraph",
  "license": "UNLICENSED",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:goerli": "graph deploy --studio fractal-test subgraph.goerli.yaml",
    "deploy:sepolia": "graph deploy --studio fractal-sepolia subgraph.sepolia.yaml",
    "deploy:mainnet": "graph deploy --studio fractal subgraph.mainnet.yaml",
    "create-local": "graph create --node http://localhost:8020/ fractal-test",
    "remove-local": "graph remove --node http://localhost:8020/ fractal-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 fractal-test",
    "test": "graph test",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.48.0",
    "@graphprotocol/graph-ts": "0.29.3"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.47.0",
    "@typescript-eslint/parser": "^5.47.0",
    "eslint": "^8.22.0",
    "eslint-config-airbnb-typescript": "^17.0.0",
    "eslint-config-next": "^13.2.4",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-prettier": "^4.2.1",
    "matchstick-as": "0.5.0",
    "prettier": "^2.7.1"
  }
}
