{
  "name": "fractal-subgraph",
  "license": "UNLICENSED",
  "version": "0.2.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:goerli": "graph deploy --studio fractal-goerli subgraph.goerli.yaml",
    "deploy:sepolia": "graph deploy --studio fractal-sepolia subgraph.sepolia.yaml",
    "deploy:base-sepolia": "graph deploy --studio fractal-base-sepolia subgraph.base-sepolia.yaml",
    "deploy:mainnet": "graph deploy --studio fractal-mainnet subgraph.mainnet.yaml",
    "deploy:polygon": "graph deploy --studio fractal-polygon subgraph.polygon.yaml",
    "create-local": "graph create --node http://localhost:8020/ fractal-goerli",
    "remove-local": "graph remove --node http://localhost:8020/ fractal-goerli",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 fractal-goerli",
    "test": "graph test",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.69.1",
    "@graphprotocol/graph-ts": "^0.34.0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.47.0",
    "@typescript-eslint/parser": "^5.47.0",
    "eslint": "^8.22.0",
    "eslint-config-airbnb-typescript": "^17.0.0",
    "eslint-config-next": "^13.2.4",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-prettier": "^4.2.1",
    "matchstick-as": "0.5.0",
    "prettier": "^2.7.1"
  }
}
